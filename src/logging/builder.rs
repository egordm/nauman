use crate::config::LoggingConfig;
use crate::execution::ExecutionContext;
use crate::logging::LoggingSpec;


// pub struct Logging {
//     pub config: LoggingConfig,
// }
//
// impl Logging {
//     pub fn spec(&self, context: &ExecutionContext) -> LoggingSpec {
//
//     }
// }